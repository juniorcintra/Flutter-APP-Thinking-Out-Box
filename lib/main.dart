import 'package:flutter/material.dart';

void main (){

  runApp(MaterialApp(
    title: "Thinking Out Box",
    home: Column(
      children: <Widget>[
      Text(
          "Texto 1",
        style: TextStyle(

        ),
      ),
    ],),
    //home: Container(color: Colors.white,),
  ));
}